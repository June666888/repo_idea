<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.june.dao.CourseMapper">
    <!--多条件课程列表查询-->
    <select id="findCourseByCondition" parameterType="com.june.domain.CourseVO" resultType="com.june.domain.Course">
        select * from course
        <where>
            <if test="courseName !=null and courseName !=''">
                and course_name like concat('%',#{courseName},'%')
            </if>
            <if test="status !=null and status !=''">
                and status =#{status}
            </if>
            <if test="true">
                and is_del!=1
            </if>
        </where>
    </select>

    <!--新建课程信息-->
    <insert id="saveCourse" parameterType="com.june.domain.Course">
        INSERT INTO course(
            course_name,
            brief,
            preview_first_field,
            preview_second_field,
            course_img_url,
            course_list_img,
            sort_num,
            price,
            discounts,
            sales,
            discounts_tag,
            course_description_mark_down,
            create_time,
            update_time
        ) VALUES(#{courseName},#{brief},#{previewFirstField},#{previewSecondField},#{courseImgUrl},#{courseListImg},#{sortNum},
                 #{price},#{discounts},#{sales},#{discountsTag},#{courseDescriptionMarkDown},#{createTime},#{updateTime});

        <!--获取添加成功记录返回的id值赋值给Course实体中的id属性-->
        <!--resultType：id是int类型
            order：表明<selectKey>里的语句是在<insert>语句执行之前还是之后
            keyProperty：把查询出来的id值封装到Course实体中的哪个属性上
            SELECT LAST_INSERT_ID();：获取最后一条插入记录对应的id值
            -->
        <selectKey resultType="int" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID();
        </selectKey>
    </insert>

    <!--新增讲师信息-->
    <insert id="saveTeacher" parameterType="com.june.domain.Teacher">
        INSERT INTO teacher(
        course_id,
        teacher_name,
        POSITION,
        description,
        create_time,
        update_time
        ) VALUES(#{courseId},#{teacherName},#{position},#{description},#{createTime},#{updateTime});
    </insert>

    <!--课程信息回显  public CourseVO findCourseById(Integer id);-->
    <select id="findCourseById" parameterType="int" resultType="com.june.domain.CourseVO">
        SELECT
            c.*,
            t.`teacher_name` teacher_name,
            t.`position` position,
            t.description description
        FROM
            course c
            left join teacher t
                on c.id = t.course_id
        where c.id = #{id}
    </select>

    <!--更新课程信息  public void updateCourse(Course course);-->
    <update id="updateCourse" parameterType="com.june.domain.Course">
        UPDATE course
        <trim prefix="SET" suffixOverrides=","><!--去掉多余的逗号-->
            <!--这些<if>就是判断前台传递过来的表单项是否为空-->
            <if test="courseName != null and courseName != ''">
                course_name = #{courseName},
            </if>
            <if test="brief != null and brief != ''">
                brief=#{brief},
            </if>
            <if test="previewFirstField != null and previewFirstField != ''">
                preview_first_field=#{previewFirstField},
            </if>
            <if test="previewSecondField != null and previewSecondField != ''">
                preview_second_field=#{previewSecondField},
            </if>
            <if test="courseImgUrl != null and courseImgUrl != ''">
                course_img_url=#{courseImgUrl},
            </if>
            <if test="courseListImg != null and courseListImg != ''">
                course_list_img=#{courseListImg},
            </if>
            <if test="sortNum != null and sortNum != ''">
                sort_num=#{sortNum},
            </if>
            <if test="price != null and price != ''">
                price=#{price},
            </if>
            <if test="discounts != null and discounts != ''">
                discounts=#{discounts},
            </if>
            <if test="sales != null and sales != '' or sales==0">
                sales=#{sales},
            </if>
            <if test="discountsTag != null and discountsTag != ''">
                discounts_tag=#{discountsTag},
            </if>
            <if test="courseDescriptionMarkDown != null and courseDescriptionMarkDown != ''">
                course_description_mark_down=#{courseDescriptionMarkDown},
            </if>
            <if test="updateTime != null">
                update_time=#{updateTime},
            </if>
        </trim>
        <where><!--若id不为空，根据id进行更新-->
            <if test="id!=null and id != '' ">id=#{id}</if>
        </where>
    </update>

    <!--更新讲师信息-->
    <update id="updateTeacher" parameterType="com.june.domain.Teacher">
        UPDATE teacher
        <trim prefix="SET" suffixOverrides=",">
            <if test="teacherName != null and teacherName != ''">
                teacher_name = #{teacherName},
            </if>
            <if test="position != null and position != ''">
                position = #{position},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="updateTime != null">
                update_time=#{updateTime}
            </if>
        </trim>
        <where>
            <if test="courseId != null and courseId != '' ">
                course_id = #{courseId}
            </if>
        </where>
    </update>

    <!--修改课程状态  public void updateCourseStatus(Course course);-->
    <update id="updateCourseStatus" parameterType="com.june.domain.Course">
        update course set status =#{status},update_time=#{updateTime} where id=#{id}
    </update>
</mapper>